name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: g++ -std=c++17 -o app src/main.cpp && echo 'Build OK'
      - run: g++ -std=c++17 -o test_app tests/test_main.cpp && ./test_app
